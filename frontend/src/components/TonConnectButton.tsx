import { TonConnectButton } from '@tonconnect/ui-react';

/**
 * TON Connect wallet button component
 * Uses the official TonConnectButton from @tonconnect/ui-react
 */
export function TonWalletButton() {
  return (
    <TonConnectButton
      className="ton-connect-button"
    />
  );
}
